# GCC compiler
CC=g++

# compilation flags (none)
LDFLAGS=

# source file
SOURCE=./hello_serial.cpp

# binary name
EXEC=hello_serial.exe

all:
	$(CC) $(LDFLAGS) $(SOURCE) -o $(EXEC)
	@echo -e "----- COMPILATION DONE -----"

clean:
	rm -r $(EXEC)

