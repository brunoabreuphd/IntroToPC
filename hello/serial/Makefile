# GCC compiler
CC=g++

# compilation flags (none)
LDFLAGS=

# source file
SOURCE=./hello_serial.cpp

# binary name
EXEC=a.out

all:
	$(CC) $(FDFLAGS) $(SOURCE) -o $(EXEC)
	@echo -e "----- COMPILATION DONE -----"

clean:
	rm -r $(EXEC)

